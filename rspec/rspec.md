# RSpec

```bash
gem install rspec
cd rspec
rspec sum_rspec.rb --format doc

# Sum
#   #calculate
#     returns 1 for sum(0, 1)
#     returns 55 for sum(0, 10

# Finished in 0.00106 seconds (files took 0.06972 seconds to load)
# 2 examples, 0 failures
```
